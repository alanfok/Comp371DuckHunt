#version 330 core

// Interpolated values from the vertex shaders
in vec3 TexCoords;

//Texture samplers
uniform samplerCube skybox;

// Ouput data
out vec4 color;


void main()
{
	//linearly interpolate between textures
	color=texture(skybox , TexCoords);
}